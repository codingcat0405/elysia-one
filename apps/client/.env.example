# Base URL of the Elysia API. Falls back to http://localhost:3000 when unset.
# Also doubles as the Better Auth client's `baseURL` (src/lib/auth-client.ts) —
# no separate var for the auth endpoint, it's the same origin as the API.
VITE_API_URL=http://localhost:3000
